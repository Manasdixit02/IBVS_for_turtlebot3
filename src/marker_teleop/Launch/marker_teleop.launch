<launch>
  <arg name="model" default="$(env MODEL)"/>
  <param name="model" value="$(arg model)"/>

  <!-- marker_teleop_key already has its own built in velocity smoother -->
  <node pkg="marker_teleop" type="marker_teleop" name="marker_teleop_keyboard"  output="screen">
  </node>
</launch>
